# JetBrains IDEs
.DS_Store/
.DS_Store
.idea/
.clwb/
.ijwb/

# Crash log files
crash.log

# Helm
**/.dev/*

# Terraform
**/.terraform.lock.hcl
**/.terraform/*
**/.terraform**
.terraformrc
terraform.rc

# .tfstate files
*.tfstate
*.tfstate.*

# Exclude all .tfvars files
*.tfvars

# Ignore override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json


# NPM
node_modules/
yarn-error.log

# Rust - CLion ignores Bazel and loads the project with Cargo, creating `target/` near the root Cargo.toml
target/

# Ignore locally generated secret.
infrastructure/k8s/**/id/secret.yaml

# Ignore all bazel-* symlinks. There is no full list since this can change
# based on the name of the directory bazel is cloned into.
/bazel-*

# TODO: these will be removed or added to a project .gitignore
# https://github.tri-ad.tech/cityos-platform/cityos/pull/3816
ns/demo/temperature-svc/deploy/*
ns/notification/deploy/*

# CI profiling data
*profile.gz
*execution_log.json
*build_event.json

# Chart dependencies
**/charts/*
**/requirements.lock
**/Chart.lock

# Test coverage
**/cover.out

# Ignore all the possible keys and certificates added to repo for testing purposes (and not)
*.key
*.crt
*.csr

## Testing kustomize for dev (kustomize build . > _test_kustomize.yaml; k apply  -f _test_kustomize.yaml --dry-run=server )
_test_kustomize.yaml

#Ignore Python venv
/venv/*

# Ignore the rust-project.json file that is autogenerated for rust analyzer
# it contains local temporary paths and shouldn't be checked in
rust-project.json

# Snyk Scan
dep-graph.json
vulnerabilities.json
vulnerabilities.sarif

# Optional .bazelrc Files
.bazelrc-ci
user.bazelrc

# envrc files
.envrc

# k8s diff files
changed.txt
infra/k8s/*.yaml
infra/k8s/*.paths.txt

# lcov output
genhtml/

# MacOS
.DS_Store
